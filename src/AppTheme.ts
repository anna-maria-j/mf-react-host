import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#0028a0",
      light: "#0028a0",
      dark: "#0028a0",
      contrastText: "#fff",
    },
    background: {
      default: "#0028a0",
    },
  },
});
